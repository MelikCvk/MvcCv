
@using PagedList
@using PagedList.Mvc
@model IPagedList<MvcCv.Models.Entity.TblAdmin>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<section class="content">
    <div class="container-fluid">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title" style="font-size: 22px; font-weight: 600; color: #333;">Deneyim Listesi</h4>
            </div>
            <div class="card-body">
                <table class="table table-bordered table-hover">
                    <thead class="thead-light">
                        <tr>
                            <th>ID</th>
                            <th>Kullanıcı Adı</th>
                            <th>Şifre</th>
                            <th>Düzenle</th>
                            <th>Sil</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td style="background-color: #f8f9fa; color: #000;">@item.ID</td>
                                <td>@item.KullaniciAdi</td>
                                <td>@item.Sifre.Substring(0, Math.Min(50, item.Sifre.Length))...</td>
                                <td class="text-center">
                                    <a class="btn btn-info btn-sm" href="/Admin/AdminGetir/@item.ID">
                                        <i class="fas fa-pencil-alt"></i> Düzenle
                                    </a>
                                </td>
                                <td class="text-center">
                                    <a class="btn btn-danger btn-sm" href="/Admin/AdminSil/@item.ID"
                                       onclick="silOnay(event, this); return false;">
                                        <i class="fas fa-trash"></i> Sil
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

                <div class="pagination-wrapper mt-3">
                    @Html.PagedListPager(Model,
                        page => Url.Action("Index", new { page }),
                        new PagedList.Mvc.PagedListRenderOptions
                        {
                            DisplayLinkToIndividualPages = true,
                            DisplayPageCountAndCurrentLocation = false,
                            UlElementClasses = new[] { "pagination" },
                            LiElementClasses = new[] { "page-item" },
                        })
                </div>

                <a href="/Admin/AdminEkle" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Yeni Admin Ekle
                </a>
            </div>
        </div>
    </div>
</section>

